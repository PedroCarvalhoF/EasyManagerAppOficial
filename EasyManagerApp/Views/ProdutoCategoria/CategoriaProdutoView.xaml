<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EasyManagerApp.Views.ProdutoCategoria.CategoriaProdutoView"
             xmlns:cat="clr-namespace:EasyManagerApp.Dtos.ProdutoCategoria"
             x:DataType="cat:CategoriaProdutoDto"
             BackgroundColor="Transparent">
    <Border Padding="12"
            Background="White"
            StrokeThickness="0"
            StrokeShape="RoundRectangle 16"
            Margin="8,8,8,8">
        <Border.Shadow>
            <Shadow Brush="#00000022" Radius="8" Opacity="0.10" />
        </Border.Shadow>
        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="Categoria_Tapped" />
        </Border.GestureRecognizers>
        <VerticalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
            <Label Text="{Binding NomeCategoria}"
                   FontAttributes="Bold"
                   FontSize="15"
                   TextColor="#1d293d"
                   HorizontalTextAlignment="Center" />
            <!-- Adicione outros campos desejados aqui -->
        </VerticalStackLayout>
    </Border>
</ContentView>